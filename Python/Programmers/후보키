# 여러 속성으로 키를 만들 수도 있으니까 부분집합을 구해야함
# >> combination 사용
from itertools import combinations
def subset(iterable):
    # 모든 길이의 부분집합 구해서 d에 chain으로 결합해 저장
    d = chain.from_iterable(combinations(iterable,i) for i in range(1,len(iterable)+1))
    return d

def solution(relation):
    #유일성을 갖는 후보키를 찾아야! > 중복을 허용하지 않는 set을 이용하자..
   
